/*Ex - 1
Define basic types for a Player. Include properties for:
 
name (string)

age (number)

isCaptain (boolean, default to false)
 
Ex-2
Create a Player class with properties name, age, and isCaptain. Initialize the values of the said players in constructor of Player class. Add a method getPlayerInfo() that returns the player's details as a formatted string.

 */
var player;
player = {
    name: "Tilak varma",
    age: 22,
    isCaptain: true
};
console.log(player.age);
